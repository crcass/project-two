<div class="container" id = 'message-center'>
    <div class="card text-center">
      <div class="card-header">
        <ul class="nav nav-tabs card-header-tabs">
          <li class="nav-item">
            <a class="nav-link active" href="#">Job Request</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Saved Jobs</a>
          </li>
        </ul>
      </div>
      <div class="card-body">
        <h4 class="card-title">Message Center</h4>
        <div class='container' id='contact-container'>
<ul>
 {{#each Contact}}
     {{> views/contact-block}}
 {{/each}}
</ul>
</div>
      </div>
    </div>
</div>